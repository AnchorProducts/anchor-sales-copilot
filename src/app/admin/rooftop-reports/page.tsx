// Rooftop Reports is temporarily paused (Coming soon). The full implementation is
// preserved in RealRooftopReportsPage.tsx — restore by rendering it here again.
import ComingSoon from "@/app/components/ui/ComingSoon";

export const dynamic = "force-dynamic";

export default function Page() {
  return <ComingSoon title="Rooftop Reports" />;
}
