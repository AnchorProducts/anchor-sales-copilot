// Rooftop Audit Logic is temporarily paused (Coming soon). The full
// implementation is preserved in RealRooftopLogicPage.tsx — restore by rendering
// it here again.
import ComingSoon from "@/app/components/ui/ComingSoon";

export const dynamic = "force-dynamic";

export default function Page() {
  return <ComingSoon title="Rooftop Audit Logic" />;
}
